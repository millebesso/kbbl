# riksdag-2026 — spec pointer

The spec for this feature is **`docs/kbbl.md`** at the repo root. It is not duplicated here;
there is one source of truth.

Vocabulary is canonical in **`CONTEXT.md`** at the repo root. Ticket titles and bodies use it.

Section references in these tickets (§5.3, §9, …) point into `docs/kbbl.md`.

These are the **deferred** items of §10, taken in the order that makes the project answer the
question it was built for. v1 (`.scratch/kbbl-v1/`) is complete: one Formateur, one Attempt, at
most one Vote, on a four-Party Fixture. What it has never done is run the parliament §9 exists
for. Still deferred after these two, and not yet ticketed: the probe harness (§8), and batch
mode (§6).
